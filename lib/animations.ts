/** Shared "3-beat nudge" — the standardized wrong-answer gesture. Softer than the old
 * 4-8 beat shakes: three quick beats, smaller travel, shorter duration. */
export const NUDGE_ANIMATE = { x: [0, -6, 5, -3, 0] }
export const NUDGE_TRANSITION = { duration: 0.3 }
